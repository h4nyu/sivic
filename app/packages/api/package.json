{
  "name": "@sivic/api",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --noEmit false",
    "prepack": "yarn build",
    "postpack": "yarn clean",
    "test": "yarn clean && jest --collectCoverage"
  },
  "devDependencies": {
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}
