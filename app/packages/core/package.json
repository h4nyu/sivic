{
  "name": "@sivic/core",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "types": "index.ts",
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --noEmit false",
    "prepack": "yarn build",
    "postpack": "yarn clean",
    "test": "yarn clean && jest --collectCoverage"
  },
  "dependencies": {
    "@charpoints/core": "^0.1.7",
    "dayjs": "^1.9.4",
    "lodash": "^4.17.20",
    "uuid": "^8.3.1"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.0",
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "typescript": "^4.0.3"
  }
}
